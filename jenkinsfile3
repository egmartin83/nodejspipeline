pipeline {
    agent {label 'jenkins-agent' }
    stages {
        stage('Initialize') {
            steps {
                script {
                    def dockerHome = tool 'docker-inst'
                    env.PATH = "${dockerHome}/bin:${env.PATH}"
                }
            }
        }
        stage('build') {
            steps {
                sh 'docker --version'
            }
        }
    }
}
